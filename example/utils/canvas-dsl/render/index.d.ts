import { CanvasStyle, Context } from '../types';
/**
 * 绘制节点
 * @param ctx canvas context
 * @param element 元素样式
 * @param wrapper 容器样式
 */
export declare function draw(ctx: Context, element: CanvasStyle, wrapper?: CanvasStyle): void;
